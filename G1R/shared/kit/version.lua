-- the kit's OWN semantic version, independent of any mod's version. Bump the
-- minor on an additive change; a breaking change renames the module (kit2).
-- Consumers log this alongside their own version and may assert a minimum.
return "1.4.0"
