# LockpickSettings 2.7 release

Packaged builds for the 2.7 line.

## Contents

```
2.7/
  LockpickSettings-2.7-alpha6.zip    earlier build
  LockpickSettings-2.7.8-alpha.zip   the build (later 2.7.x builds land here too)
```

## Live mod page

The Nexus mod page content (description, summary, preview and the raw-linked
images) lives in its own folder, not here: see
[`../../nexus-page/`](../../nexus-page/). That folder is prod, mirrors the current
live page, and keeps stable image URLs across builds.
